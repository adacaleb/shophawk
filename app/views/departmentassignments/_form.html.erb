<%= form_with(model: departmentassignment) do |form| %>
  <% if departmentassignment.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(departmentassignment.errors.count, "error") %> prohibited this departmentassignment from being saved:</h2>

      <ul>
        <% departmentassignment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :departmentassignment, style: "display: block" %>
    <%= form.text_field :departmentassignment %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
